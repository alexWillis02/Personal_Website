---
import Base from "../../layouts/Base.astro";
import { getProjects } from "../../lib/content";
import { url } from "../../lib/utils";

const projects = await getProjects();
---

<Base title="Projects" description="Things I've built.">
  <div class="container">
    <header class="page-head">
      <p class="eyebrow">Sec-02 // Projects</p>
      <h1>Projects</h1>
      <p>Hardware I've designed, built, and tested, plus the analysis and code behind it.</p>
    </header>

    {projects.length === 0 && <p class="empty">Nothing here yet.</p>}

    <ul class="grid">
      {
        projects.map((p) => (
          <li class="card bracket">
            <div class="top">
              <span class={`status ${p.data.status}`}>{p.data.status}</span>
              {p.data.kind && <span class="kind">{p.data.kind}</span>}
            </div>
            <h2>
              <a href={url(`/projects/${p.id}/`)}>{p.data.title}</a>
            </h2>
            <p>{p.data.description}</p>
            {p.data.stack.length > 0 && (
              <ul class="tags" aria-label="Tech stack">
                {p.data.stack.map((s) => (
                  <li class="tag">{s}</li>
                ))}
              </ul>
            )}
          </li>
        ))
      }
    </ul>
  </div>
</Base>

<style>
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(min(100%, 20rem), 1fr));
    gap: 1rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .card {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    padding: 1.35rem;
    --b-color: var(--faint);
    transition: transform 0.15s ease;
  }

  .card:hover,
  .card:focus-within {
    --b-color: var(--accent);
    transform: translateY(-2px);
  }

  .kind {
    margin-left: auto;
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--faint);
  }

  .top {
    display: flex;
    gap: 0.5rem;
    align-items: center;
  }

  .status {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--muted);
  }

  .status::before {
    content: "●";
    margin-right: 0.35rem;
  }

  .status.active::before {
    color: var(--accent);
  }

  .status.shipped::before {
    color: var(--accent-2);
  }

  h2 {
    margin: 0;
    font-size: 1.25rem;
    font-weight: 600;
  }

  /* Make the whole card clickable via the title link. */
  h2 a {
    color: var(--text);
    text-decoration: none;
  }

  h2 a::after {
    content: "";
    position: absolute;
    inset: 0;
  }

  p {
    margin: 0;
    color: var(--muted);
    flex: 1;
  }
</style>
